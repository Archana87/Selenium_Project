package com.NewPackage;

import static org.testng.Assert.assertEquals;

import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

public class ClassonJavaTest {
//ClassonJava java = new ClassonJava();
 // @Test(dataProvider="dpaddition")
  /*public void add1newTest(int exp_value, inta, intb) {
    Assert.assertEquals(a, b, exp_value);
    System.out.println("Addition");
  
  @DataProvider(name="dpaddition")
  //public object[][]getdata(){
	 // return new object[][] {(10,5,5)(20,10,5)(50,25,25)}
  }

}*/
}